<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:icon="@drawable/logo_min"
        android:label="Ghosts of history">

        <activity
            android:name=".persistentcloudanchor.EditActivity"
            android:exported="false" />
        <activity
            android:name=".persistentcloudanchor.AnchorListActivity"
            android:exported="true"
            android:label="@string/title_activity_anchor_list"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>